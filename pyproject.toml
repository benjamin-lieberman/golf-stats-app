[tool.poetry]
name = "golf-stats-app"
version = "0.1.0"
description = "Golf stats tracking app"
authors = ["Ben"]

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
